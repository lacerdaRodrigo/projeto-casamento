// Flags de UI temporárias — fáceis de reverter num lugar só.

/**
 * Botão "+ Subtema (opcional)" na árvore. Escondido por ora (testando sem ele —
 * estava atrapalhando). Pra trazer de volta: mude para `true`. O código do
 * compositor de subtema continua no lugar, só não renderiza.
 */
export const MOSTRAR_ADD_SUBTEMA = false;
